Follow /ai/MASTER_PROMPT.md exactly.

STABILIZATION PASS ONLY (no new features).

1) Read:
- /ai/SCOPE_CONTRACT.md
- /docs/ARCHITECTURE.md

2) Detect and report:
- stack
- current structure (core/features/services/ui)
- risky config hotspots (firebase/auth/env)
- duplicate logic or circular dependencies (high level)

3) Run verification:
- scripts/check.sh (mac/linux) OR scripts/check.ps1 (windows)
- If those are not usable, run closest available npm scripts:
  lint, typecheck, test, build

4) Fix only:
- failures caused by missing wiring
- clear misconfigurations blocking checks
- issues directly caused by your edits

5) Do not refactor broadly. Do not reorganize folders.

6) Append results to /ai/AI_CHANGELOG.md.
If you see a recurring issue pattern, add to /ai/KNOWN_ISSUES.md.
